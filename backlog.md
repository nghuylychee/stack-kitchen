<!-- Flat ticket list. No epics, no sprints, no estimates.
     Order: breaks online play > blocks the next playtest > cheap and visible > everything else. -->

# Backlog — Stack Kitchen

> Tickets #1–#7 were built in the incubator (slot `007-kitchen-mahjong`, single-file
> prototype, now `design/reference-prototype.html`). From #8 on, code lives in `src/`.

## Open

| # | Ticket | Design doc |
|---|---|---|

## Done

| # | Ticket | Built | Notes |
|---|---|---|---|
| 1 | Prototype core loop: pool 108 thẻ + chia 13, Draw/Reveal, Play/Claim theo ưu tiên điểm, 3 loại món + thưởng +10, 2 cách kết thúc, bot AI + log lý do + Show AI hands | 2026-09-16 | `design/01-`…`05-`. Verified qua browser: chia đúng (4 người pool 56, 2 người pool 82); ván 4 người kết thúc A (Bot 3 đủ 3 loại, +10, turn 24, 5 lần tố); ván 2 người kết thúc B (pool rỗng, turn 82); tranh tố hoà điểm → người gần theo chiều kim đồng hồ thắng (Bot 1 hơn Bot 2); bot giữ bài ("holding for Bun Bo Hue, waiting Chili") và sau đó reveal Bun Bo Hue; popup Claim/Pass hiện cho người thật; Restart giữa ván sạch; console không lỗi. Người thật chỉ được test bằng script (bốc/đánh thẻ đầu tiên) — chưa ai chơi bằng tay. |
| 2 | Rework visual/UX: bàn trà đá vỉa hè, ghế theo số người (2/3/4, clockwise), tay bài xoè quạt + hover/kéo nghiêng, thẻ bay cong, deal animation, Bếp stack → Cook → progress `COOK_MS` → thẻ món → thu vào khu món + điểm bay; tố bằng kéo thẻ vào Bếp + Pass (bỏ popup claim); bot hiện cùng chuỗi ở ghế | 2026-09-16 | `design/ui/table.md`, `design/art.md`, `02-` Rule 9–14, `03-` Rule 11–14. Logic luật/AI/số giữ nguyên. Verified qua browser: layout 2/3/4 người ở 1280×800 và 4 người ở 1024×700 không đè nhau; lượt người thật: bốc → kéo Rice Noodle+Beef vào Bếp hiện Cook Pho Bo +2 (không tự nấu), thêm Chili (Cook ẩn) + Lemongrass → Cook Bun Bo Hue +7 → progress → thẻ món → kéo vào khu món → 7 pts; thả Chicken sai → bật về; đánh bằng kéo và bằng chọn+Play; tố Bean → Che Ba Mau thắng, thu món, tiếp tục không bốc; Pass hoạt động; ván Fast chạy tới màn kết thúc (Bot 2 đủ 3 loại, turn 36, 8 lần tố); console không lỗi. Chưa verify: thua ưu tiên khi tố (toast + thẻ bay sang ghế thắng), Restart giữa animation, resize giữa animation, tay rỗng sau reveal, pool cạn. Thêm `?players=2|3|4` trên URL. Giả định: dock chia cột [đầu ghế][Bếp][món + tay] nên quạt tay hẹp hơn `min(1100, W−80)` nhưng vẫn ≥44px/thẻ ở 1024px; Bếp chỉ nhận 1 bản mỗi loại; ván chạy tới màn kết thúc được kiểm bằng cờ tạm cho AI chơi thay ghế người (đã gỡ khỏi file). |
| 3 | Đơn giản hoá HUD + ghế: bỏ Speed, Show AI hands, đếm Pool/Claims/Discard ở HUD, "Foods N" + badge "×N" ở ghế bot, nhãn Clockwise, text "Cooked foods land here" | 2026-09-16 (2) | `design/ui/table.md`, `design/05-ai-player.md`. Verified qua browser (1280×800, 4 người): HUD chỉ còn Players/Turn/Restart/Recipes/Log; nhãn `Pool N` dưới chồng bài vẫn hiện; ghế bot chỉ còn tên/điểm/A-M-D/×N 1 lần; không còn Clockwise; `#my-foods` rỗng không chữ; grep file không còn id/nhãn đã bỏ. `AI_STEP_MS` cố định 900. Log bot giờ luôn kèm lý do khi đánh thẻ (trước chỉ hiện khi bật Show AI hands). Giả định: nhãn dưới đống bỏ đổi từ "Discard N" thành "Discard" (không số) cho khớp yêu cầu bỏ đếm Discard. |
| 4 | Stack trên tay thay Bếp: ghép thẻ trong quạt → Cook → progress → thẻ món → kéo vào `#my-panel`; tố = kéo thẻ phát sáng lên thẻ/stack trong tay; bot dùng `.seat-stack`; món đã thu hiển thị dạng thẻ món nhỏ | 2026-09-16 (2) | `design/02-draw-reveal.md` Rule 9–15, `design/03-play-claim.md` Rule 1, 11–15, `design/ui/table.md`, `design/art.md`. Luật/AI/điểm/kết thúc giữ nguyên. Verified qua browser (4 người, 1280×800 và 1024×700): Beef lên Rice Noodle → stack lệch dọc hiện Cook Pho Bo +2 (không tự nấu); thêm Chili → Cook ẩn; thêm Lemongrass → Cook Bun Bo Hue +7 → progress → thẻ món tại đúng ô → kéo vào panel → 7 pts, badge M sáng, thẻ món nhỏ 64×64 trong `#my-foods`; Bread lên Rice Noodle → bật về + rung + toast; tách thẻ giữa stack ra khoảng trống → thành thẻ rời tại vị trí thả; đánh thẻ bằng kéo ra giữa và bằng chọn + nút Play; tố Gac Fruit vào Sticky Rice → Cook Xoi Gac +2 → hoà điểm, Bot 3 gần hơn thắng → toast, chỉ thẻ tố bay đi, Sticky Rice vẫn trên tay; bot hiện mini stack + progress + khói cạnh ghế, món bay vào `.seat-foods` dạng thẻ nhỏ; ván chạy tới màn kết thúc (Bot 2 đủ 3 loại, lật bài bot) bằng hook tạm cho AI chơi thay ghế người (đã gỡ); console không lỗi. Chưa verify: thắng tố (tự thu món), 2 stack cùng khớp liền kề (nút Cook so le), stack 4 thẻ tràn mép dock ở mọi vị trí, Restart/resize giữa animation, tay rỗng sau reveal, pool cạn, tab blur. Giả định: thắng tố tự thu món (theo ui/table.md, không cần kéo); nút Cook chỉ bị giới hạn rộng `min(150, spacing×0.95)` khi ô liền kề cũng có Cook — nếu không thì hiện đủ tên (spacing 14 thẻ ~73px làm chữ thành "Coo…"); lệch dọc stack dùng 22px theo ui/table.md (art.md ghi 12px); còn thẻ đang nấu hoặc thẻ món chưa thu thì chặn Play kèm toast; ghép/tách stack chỉ được trong lượt mình (sau Draw) hoặc cửa sổ tố. |
| 5 | Chia 11 thẻ; quạt bài khít hơn; stack để nấu chuyển từ trên tay ra mặt bàn (`#prep`), tố cũng qua stack trên bàn | 2026-09-16 (3) | `design/01-card-pool-deal.md`, `design/02-draw-reveal.md` Rule 9–15, `design/03-play-claim.md` Rule 1/11/13, `design/ui/table.md` Tuning pass (3), `design/00-core.md` Controls. Verified qua browser (4 người 1280×800): chia 11, pool 64; kéo Rice ra bàn → stack mới; Chicken lên Rice → nhập; Crab lên Rice+Chicken → bật lại; tố Milk bằng kéo ra bàn + Coffee Bean + Ice → Cook Ca Phe Sua Da +4 → thắng tố, tự thu, 4 pts, badge D; kéo Beef ra bàn rồi tap → về tay; đánh Chili bằng kéo vào giữa; layout 2/3/4 người ở 1280×800 và 4/3 người ở 1024×700 không đè; console không lỗi. Chưa verify: thua ưu tiên khi tố, 2 stack kề cùng Cook, stack 4 thẻ ở 1024×700 (tính ra tràn ~7px trên/dưới `#prep`), nhiều stack làm hàng nén, thu thẻ món bằng tap từ bàn, Restart giữa animation. Giả định: stack neo trái thay vì căn giữa; bot vẫn stack cạnh ghế như cũ. |
| 6 | Quạt tay căn theo tâm bàn (không theo hộp chứa riêng); stack để nấu trên bàn đổi từ chồng dọc sang quạt cong nằm ngang, slot quanh tâm bàn; animation đóng quạt → food card lúc Cook xong; đổi tên hiển thị "Kitchen Mahjong" → "Stack Kitchen" | 2026-09-16 (4) | `design/ui/table.md` Tuning pass (4), `design/00-core.md`. Verified qua browser (4 người, 1280×800 và 1024×700): quạt tay luôn căn giữa bàn kể cả ngay sau khi đánh 1 thẻ; kéo Pork ra bàn rồi Herb → quạt ngang 2 thẻ nghiêng đối xứng; mở stack thứ 2 (Sticky Rice) ở slot lệch phải, stack đầu không xê dịch; ghép Bread+Herb+Pork → Cook Banh Mi +4 → quạt đóng lại (scaleX co về tâm) rồi hiện thẻ món đúng kích thước gốc (bug đầu tiên: `fill:'forwards'` của animation đóng quạt đè kích thước lên thẻ món kế tiếp — đã sửa bằng `anim.cancel()` sau khi đóng quạt xong) → thu vào panel, +4 điểm; tab title/HUD/README/hub đổi đúng "Stack Kitchen"; console không lỗi. Chưa verify: 3 stack đồng thời (slot thứ 3, lệch trái), thua ưu tiên khi tố qua stack quạt ngang, animation đóng quạt bị Restart giữa chừng. |
| 7 | Vùng nấu thu nhỏ còn 1 ô 250px giữa bàn, chỉ 1 stack; bỏ chạm chọn + nút Play, ô đánh giữa bàn phát sáng suốt bước Play + sáng mạnh khi kéo vào | 2026-09-16 (5) | `design/ui/table.md` Tuning pass (5), `design/02-draw-reveal.md` Rule 9/13/14, `design/03-play-claim.md` Rule 1/11, `design/00-core.md` Controls. Verified qua browser (4 người 1280×800, layout 1024×700) bằng pointer event giả lập: vùng nấu 250px tâm x=640 = tâm bàn; ô đánh hiện "Drag here to play" + viền phát sáng khi tới bước Play; chạm thẻ → không còn nút Play, ô đánh nháy; kéo thẻ → vùng nấu hiện viền gạch, qua ô đánh → sáng mạnh, thả → đánh ra; Vermicelli vào vùng → stack; Herb thả ở góc vùng → nhập cùng stack; Crab → bật về; Fish Sauce → nhập (3 thẻ, 1 stack); Bean thả ngoài vùng trên bàn → bật về; console không lỗi. Chưa verify: tố qua vùng nấu 1 stack, thẻ món chưa thu chặn thẻ mới, nấu xong stack 4 thẻ trong vùng 250px. |
| 8 | Tốt nghiệp sang repo riêng: Vite + TS, tách luật thành `Match` host-authoritative dùng chung cho 2 chế độ, home screen (Play vs Bots · Create room · Join room), lobby 2–4 ghế có bot, chơi online PvP qua PeerJS (cửa sổ tố đồng thời, giới hạn lượt, rớt mạng → bot, vào lại lấy ghế, Play again, host rời → đóng phòng), deploy GitHub Pages | 2026-09-16 | `design/06-online-room.md`, ADR-004 (repo studio). Verified qua browser (1280×800): Play vs Bots — chia bài, thua tố (Bot 1 Banh Chung 4 > Xoi Gac 2, thẻ tố bị gỡ), thắng tố Com Tam, nấu Xoi Gac → thu món → 6 pts, đánh thẻ; online 2 tab (host Huy + Lan qua `?room=`) — lobby, 3 ghế + 1 bot, mỗi máy tự ở ghế dưới, ván chạy trọn 52 lượt tới khi Bot 2 đủ 3 loại, cả 2 người đều tố thành công qua mạng, non-host thấy "Waiting for host…", Play again chạy cả 2 máy; reload tab Lan giữa ván → bot đánh thay → nhập lại mã → lấy lại ghế sau ~2,4s còn 8 pts + 2 món; Room full; host về Home → client "Host left — room closed"; `npm run build` sạch; console không lỗi. Sửa trong lúc test: host bị kéo lại bàn chơi sau khi đóng phòng; 2 tab chung localStorage giành ghế nhau; animation đóng quạt treo khi pane ẩn. Chưa verify: timeout lượt 30s, timeout tố 8s, 2 người thật cùng tố 1 thẻ, rớt mạng đang nấu, Play vs Bots tới màn kết thúc, khác mạng (NAT/4G), màn hình điện thoại, deploy Pages thật (chưa có remote). |
| 9 | Luật Menu & Order trong `Match`: Menu `MENU_SIZE` 6/8/10, Order riêng `ORDER_SIZE` 3/4/5 (được trùng), pool chỉ nguyên liệu của Menu × `COPIES_PER_TYPE_MENU`, Kết thúc A' (xong Order → +`ORDER_BONUS`, dừng ngay), `View` chỉ chứa Order ghế mình, Order mọi ghế gửi lúc kết thúc, host `wait` intro Menu trước deal, bump `PREFIX` v2 | 2026-09-19 | `design/07-menu-orders.md`, `01-`, `06-` → BUILT; `07-`, `00-`, `03-`, `04-` giữ AGREED (còn #10/#11). Verified bằng script tạm chạy `Match` thật (tua nhanh thời gian, không nằm trong repo): Menu 6/8/10 và Order 3/4/5 đúng theo 2/3/4 người; mọi Order nằm trong Menu, không lặp trong 1 Order; pool = đúng các loại của Menu × 6 (vd 12/14/17–20 loại → 72/84/102–120 thẻ); ~700 `View` gửi đi không cái nào chứa `order`/`hand` người khác trước khi kết thúc; ván cố ý đủ Order → "Order 1/3…3/3 done", +10, `reason: 'order'`, dừng ngay; payload kết thúc có Order mọi ghế; nấu món ngoài Order cộng điểm, không tính vào Order; cook món ngoài Menu → reject "Not on the menu". Browser (1280×800): Play vs Bots chạy, log hiện Menu + "Your order", bảng Recipes chỉ còn món Menu; online 2 tab (host + `?room=`) — cùng Menu, Order mỗi tab khác nhau (có trùng món), DOM tab khách không chứa Order host, reload tab khách → vào lại vẫn đúng Order; console không lỗi. Client tối thiểu: ghép stack/useful/Recipes/màn kết thúc dùng Menu, cột "Courses" → "Order" (✓/✕). **Chưa verify:** màn kết thúc render trên browser (chỉ kiểm payload), tố xong đủ Order kết thúc ván (`03-` edge case — cùng code path với reveal nhưng chưa chạy), timeout lượt/tố online. **Phát hiện:** bot chưa theo Order (#11) nên gần như không ai xong Order — 40 ván bot: 2 người 6/40, 3–4 người 0/40, còn lại hết pool (54–68 lượt); đợi #11 rồi mới đánh giá `COPIES_PER_TYPE_MENU`. Host đã chờ intro Menu 2,6s + pop-in nhưng client chưa có overlay (#10) nên đầu ván có 1 khoảng lặng. |
| 10 | UI Menu & Order: overlay `#menuIntroModal` (tap để bỏ qua, chỉ ở máy đó; đếm giờ dừng khi tab ẩn), dải `#menu-rail` + popover công thức (hover 150ms / tap), `#my-order` 3–5 dòng (chip có/chưa, ✓ viền vàng nhạt + glow 1 nhịp + toast "Order N/M done"), badge tím `--order-need` trên thẻ trong tay, bỏ huy hiệu A/M/D ở mọi ghế, nút Recipes → "Menu" (chỉ N món), `#endModal` cột Order (ảnh 32px + ✓/✕) + nhãn "Finished order first", câu giới thiệu ở Home theo luật mới | 2026-09-19 | `design/ui/menu-orders.md`, `ui/table.md`, `art.md` (mục Menu & Order), `04-` → BUILT. Verified qua browser: intro hiện đủ 10 món trước deal, tự đóng, tap → đóng ngay; rail 10 món cố định; popover Chao Ga hiện Rice mờ, Chicken/Water sáng; 5 dòng Order, chip sáng đúng thẻ trong tay, badge tím đúng 6 thẻ phục vụ Order; kéo Gac Fruit + Sticky Rice → Cook Xoi Gac → thu → dòng Xoi Gac ✓ + viền vàng + toast "Order 1/5 done" + badge Gac Fruit tắt, điểm +2; ván 2 người chơi tới hết pool (script tạm tự bốc/đánh, đã gỡ) → màn kết thúc có cột Order ✓/✕ cho cả 2 người; online 2 tab — cùng rail, Order mỗi tab khác nhau, reload tab khách → rail + Order dựng lại sau ~0,7s, không phát lại intro/pop-in; layout đo bằng JS: 4 người 1280×800 và 1024×700, 3 người 1024×700 — `#my-order` nằm trọn trong `#my-panel`, không cuộn; console không lỗi. **Chưa verify trên browser:** nhãn "Finished order first" (logic kết thúc theo Order đã kiểm ở #9, chưa có ván UI nào xong Order), popover bằng tap trên máy cảm ứng. **Lệch doc (GUESS):** số thẻ úp dời lên cùng hàng tên/điểm (bỏ `#my-h3`) và khoảng cách dòng 2px — không thì 5 dòng Order tràn `#my-panel`; ở 1024×700, 5 dòng cao 24px thay vì 26px. **Vấn đề cần người duyệt:** ván 3–4 người, `#my-foods` gần như không còn chỗ (0–6px) — món đã nấu vẫn thấy qua ✓ Order và điểm, nhưng món ngoài Order không thấy ở panel mình. |
| 11 | Bot theo Order + đọc đối thủ: ưu tiên món trong Order (nhóm Order chưa xong trước, `ORDER_WEIGHT`), luôn reveal khi xong Order, `interest`/`danger` từ lịch sử sự kiện công khai (reveal, tố, pass khi đủ điều kiện, đánh thẻ), né đánh thẻ nuôi đối thủ (`FEED_WEIGHT`), tố-để-chặn khi rẻ (Rule 11), nấu ngoài Order để vét thẻ hiếm (Rule 12); bot chỉ nhận `BotSelf` (tay + Order của chính nó) + `BotTable` (Menu, discard, món đã ngửa, lịch sử công khai) — không bao giờ nhận `Match` | 2026-09-19 | `design/05-ai-player.md`, `07-`, `00-`, `03-` → BUILT. Verified bằng script tạm chạy `Match` thật (không nằm trong repo): 120 ván bot — xong Order: 2 người 22/40, 3 người 3/40, 4 người 0/40 (trước #11: 6/40, 0/40, 0/40), ván trung bình 41/61/66 lượt, không ván nào kẹt; 10 ván 4 người đọc log bot: 53 lần "in Order", 44 "best off-Order dish", 407 lượt đánh có "feeds rivals", 4 lần giữ bài, 1 lần "claims to deny — danger(Pork)=9"; 21 ván kết thúc bằng tố xong Order → dừng ngay, không thêm lượt/thẻ bỏ (`03-` edge case). Browser: Play vs Bots 4 người chạy, log bot kèm lý do; console không lỗi mới (1 lỗi HMR cũ từ lúc đang sửa file, đã hết sau khi tải lại). **Chưa verify:** Rule 12 (nấu ngoài Order để vét) chưa kích hoạt lần nào trong các ván đã chạy — code có, ngưỡng GUESS có thể quá chặt; 2 tab online với bot đọc 1 người thật. **Phát hiện cho `/tuning`:** với `ORDER_SIZE` 4/5 ở 3/4 người gần như không ai xong Order → ván gần như luôn hết pool, điều kiện thắng thực tế chỉ còn là điểm. |
| 12 | Tay `HAND_SIZE` = 7 thay `HAND_START` = 11; bước Refill cuối lượt (sau Play + cửa sổ tố) bốc bù về `HAND_SIZE` cho người vừa đánh — cả người bị tố và người thắng tố; tay rỗng sau reveal vẫn bốc bù; pool cạn thì dừng; event `draw` thêm cờ `refill` (bay thẻ `REFILL_STAGGER_MS` = 150ms, host `wait` cùng hằng số); bump `PREFIX` v3 | 2026-09-19 | `design/08-hand-refill.md` → BUILT; `00-core.md` (lượt 4 bước), `01-` Rule 4/7 + Numbers cập nhật theo. `npm run build` sạch. **Script tạm chạy `Match` thật** (tua nhanh thời gian, không nằm trong repo) — 90 ván có 1 ghế người do script điều khiển (tự nấu khi đủ món): mọi người đúng 7 thẻ sau chia; trước mỗi lượt, mọi tay ≥ 7 (người đang lượt ≥ 8) trừ khi pool đã cạn; không lần nào bốc bù cho tay đã đủ; pool giảm đúng số thẻ bốc; ghế người nhận đủ loại thẻ bốc bù của mình (377 thẻ / 137 lần nấu), 0 lần lộ loại thẻ bốc bù hay tay/Order của ghế khác; pool cạn giữa lúc bốc bù ở 30/90 ván → bốc bù dừng, ván kết thúc ở bước Draw kế tiếp theo Kết thúc B, không ván nào kẹt. Kết quả: xong Order 2 người 26/30, 3 người 13/30, 4 người 2/30; ván trung bình 26/35/44 lượt; mỗi lần bốc bù ~3,1 thẻ. **Browser (1280×800, Play vs Bots 4 người, thao tác qua pointer event giả lập):** chia 7, pool 92 = 120 − 28; nấu Pho Bo → tay 6, đánh Water → sau cửa sổ tố tay 5 → 7, pool 90 → 88, log `You refill +2.`; bot 1 nấu + đánh → ×4, chờ tố xong mới về ×7; tôi tố thẻ của bot 1 (Cook Chao Ga +4) → bot 1 vẫn `refills +3`; tôi đánh tiếp → `You refill +3.` đúng 1 lần ở cuối; console không lỗi. **Online 2 tab:** tab khách vào được phòng `v3`, rớt kết nối rồi vào lại lấy lại ghế, host thấy `Bot 2 refills +3.` sau khi bot 2 thắng tố. **Chưa verify trên browser:** tab khách chỉ thấy số thẻ của host +k mà không thấy loại (browser pane bị đóng giữa lúc test — chỉ kiểm được bằng script tạm qua đúng đường `emit` thật). **Phát hiện cho `/tuning`:** 4 người pool cạn nhanh, gần như không ai xong Order (2/30) — đúng mục "Theo dõi" của `08-`. |
| 13 | File config `src/core/config.ts` cho người dùng tự chỉnh (chú thích tiếng Việt, Range + doc nguồn từng dòng): `HAND_SIZE`, `MENU_SIZE`/`ORDER_SIZE` theo số người, `COPIES_PER_TYPE_MENU`, `POINTS_BY_SIZE`, `ORDER_BONUS`, `CLAIM_WINDOW_MS`, `TURN_LIMIT_MS`; `data.ts` import + re-export, bỏ định nghĩa cũ; `configErrors()` trong `data.ts` kiểm lúc `Match.start()` — sai thì `console.error` liệt kê từng lỗi + toast `Invalid config — see console`, không chia bài | 2026-09-19 | Nguồn số: `01-`, `04-`, `06-`, `07-`, `08-` (không đổi luật, không đổi doc status). `npm run build` sạch; grep `src/ui`, `main.ts`, `index.html` không còn số hardcode. Verified qua browser (Play vs Bots 3 người): config mặc định → 7 thẻ, Menu 8, Order 4, pool 16 loại × 6 − 21 = 75; sửa tạm `HAND_SIZE` 5, `MENU_SIZE[3]` 5, `ORDER_SIZE[3]` 2, `COPIES_PER_TYPE_MENU` 4 → log `5 cards each, pool 45` (15 × 4 − 15), rail Menu 5 món, `#my-order` 2 dòng, bot bốc bù về 5 (`Bot 2 refills +3`); sửa tạm sai (`HAND_SIZE` 0, `ORDER_SIZE[3]` 6 > `MENU_SIZE[3]` 5, bỏ `POINTS_BY_SIZE[4]`) → không chia bài, console báo đủ 3 dòng lỗi, toast hiện. Đã khôi phục config gốc (bundle trùng hash với bản trước khi test). **Chưa verify trên browser:** thời gian online (`CLAIM_WINDOW_MS`/`TURN_LIMIT_MS`) đổi theo config trong phòng 2 tab — chỉ chuyển chỗ khai báo, cùng tên hằng số, `tsc` sạch; animation chia bài khi pane bị ẩn (tab `hidden` → trình duyệt dừng animation, có từ trước, không do ticket này). |
| 14 | Chấm tiến độ Order ở `.seat-head` ghế đối thủ: `.opips` = `orderSize` chấm 8px, `orderDone` chấm đầu vàng (`--gold`), còn lại viền mờ, không số, không nhãn; `View.players[i]` thêm `orderDone`/`orderSize` công khai mọi ghế (tính từ `rules.ts` `orderDone()`), tên món Order vẫn chỉ gửi lúc kết thúc; ghế mình không có chấm; bump `PREFIX` v4 | 2026-09-19 | `design/09-order-progress-signal.md` → BUILT; `07-` Rule 15 sửa câu chữ ("ẩn hoàn toàn" → tên món ẩn, số đếm công khai theo `09-`). `npm run build` sạch. **Script tạm chạy `Match` thật** (không nằm trong repo) — 75 ván 2/3/4 người, 15 126 `View` gửi ghế người: `orderDone` luôn khớp số món Order đã reveal thật, `orderSize` = `ORDER_SIZE[n]`, không bao giờ giảm, chỉ tăng đúng event reveal của chính ghế đó; 339 lần nấu món ngoài Order không làm chấm tăng; 0 lần tên món Order ghế khác nằm trong payload trước khi kết thúc; 30 ván kết thúc bằng xong Order → người xong có đủ chấm sáng. **Browser (Play vs Bots 4 người, 1280×800):** đầu ván 3 ghế bot mỗi ghế 5 chấm mờ, `#my-panel` không có chấm; Bot 3 tố Xoi Gac (trong Order) → Bot 3 sáng 1 chấm vàng (ảnh chụp); Bot 1 tố Banh Xeo ngoài Order (+7) → Bot 1 vẫn 0 chấm; 1024×700: chấm nằm trọn trong pod, cách cụm số thẻ ~117px; console không lỗi. **Chưa verify trên browser:** đủ chấm lúc xong Order (chỉ kiểm bằng script), online 2 tab thấy chấm khớp nhau + sau rớt mạng/vào lại (browser pane bị ẩn liên tục → animation dừng, không chạy được 2 tab; cả 2 chế độ dùng chung `Match.view()`, script tạm đã kiểm đúng đường đó). |
| 15 | Refill ngay sau mỗi lần thu món (tự nấu, bot nấu, thắng tố, bot đánh thay lúc hết giờ) lên `HAND_SIZE`+1; bỏ Refill cuối lượt và Refill cho người bị tố | 2026-09-19 | `design/08-hand-refill.md` → BUILT, `00-core.md` (Refill nằm trong Check). Chỉ sửa `match.ts` (`refill()` đích `HAND_SIZE + 1`, gọi sau `recheck`/`settleCook`/`botCook`/thắng tố), không đổi event. `npm run build` sạch. **Script tạm chạy `Match` thật** (không nằm trong repo) — 90 ván 2/3/4 người có 1 ghế người do script điều khiển: mọi đầu lượt người vừa bốc có đúng 8, mọi người khác đúng 7 (trừ khi pool cạn); mỗi lần bốc bù đều ngay sau 1 lần thu món của chính ghế đó và dừng đúng ở 8; pool giảm khớp; 0 lần lộ loại thẻ bốc bù; không ván nào kẹt. Xong Order: 2 người 26/30, 3 người 15/30, 4 người 6/30 (trước #15: 26/13/2); ván trung bình 24/32/36 lượt. **Chuỗi nấu trong 1 lượt:** 204 lượt nấu ≥2 món, dài nhất 6 món — đúng rủi ro doc đã ghi, người dùng chấp nhận. **Browser (Play vs Bots 2 người):** nấu Xoi Gac → thu món → tay 6 → bốc bù ngay +2 → 8, log `You refill +2.` trước khi đánh → đánh → 7; 5 lượt không nấu đều 8 → 7, không bốc bù; console không lỗi. **Chưa verify trên browser:** nấu tiếp món thứ 2 bằng thẻ vừa bốc bù (chỉ kiểm bằng script); online 2 tab (event không đổi so với #12, browser pane bị ẩn liên tục). |
| 16 | Dock thành 3 cột: `#my-foods` (hộp riêng, góc trái dưới, drop-target thu món, tối thiểu 76px) · `.hand-fan` (giữa) · `#my-panel` (góc phải dưới: tên/điểm/số thẻ/`#my-order`, co theo nội dung); `hot` + hit-test thu món chuyển từ `#my-panel` sang `#my-foods`; bỏ `Restart`/`Menu`/`Log ▸` khỏi `#hud`, gỡ `#recipeModal`, `#log-drawer`, `renderRecipes()`, `log()` (host vẫn gửi `log`, client bỏ qua); giữ `#menu-rail` | 2026-09-19 | `design/ui/table.md` Tuning pass (7) → BUILT, `ui/menu-orders.md` → BUILT (mục 3, 7, hình vẽ HUD sửa theo). Doc (7) chỉnh câu chữ "neo anchor %" → "cột trái/phải của dock" cho khớp cách làm. `npm run build` sạch. Verified qua browser (Play vs Bots): HUD còn `Stack Kitchen · Turn · Home`; `#log-drawer`/`#recipeModal`/`#btnRestart`/`#btnRecipes`/`#btnLog` không còn trong DOM; `#menu-rail` đủ 10 món; ảnh chụp 1280×800 4 người: hộp món trống góc trái dưới, hộp info (viền vàng lượt mình) góc phải dưới, quạt căn giữa bàn; nấu Xoi Gac rồi Cao Lau → cả 2 thẻ món vào hộp góc trái, điểm 6, Cao Lau ✓ trong `#my-order`; đo 1024×700 với 2/3/4 người: quạt không đè 2 hộp, không ghế nào đè 2 hộp, 3/4/5 dòng Order nằm gọn trong `#my-panel`; console không lỗi. **Chưa verify:** viền sáng `hot` của `#my-foods` khi đang kéo thẻ món qua (MutationObserver không bắt được lần nào — 2 món có thể đã vào hộp qua đường tap thay vì kéo, xem #17); online (chip phòng/đồng hồ trong HUD không đổi code, chưa mở 2 tab). **Sửa sau đóng ticket (2026-09-19, người dùng gửi ảnh khoanh vùng: *"ý tôi đặt món ăn trên bàn khu vực này nè"*):** `#my-foods` rời dock, lên mặt bàn — `position:absolute` trong `#table-surface`, góc trái dưới của `#alu-table` (`left: 21% + 20px`, đáy ngay trên đầu quạt tay), rộng `clamp(140px,14vw,220px)`, nền tối trong suốt; dock còn 2 cột. Đo 2/3/4 người ở 1280×800, 1024×700, 1920×1080: hộp nằm trong mép bàn, cách đầu quạt 10px, không đè quạt/vùng nấu/cụm giữa bàn/ghế/`#my-panel` (lần đo đầu ở 1024×700 lấn vùng nấu 10px và ghế trái 4 người 6px → thu hẹp + dịch vào 20px, đo lại sạch); ảnh chụp 1280×800 đúng vùng khoanh; console không lỗi. Sau đó bỏ khung/nền theo yêu cầu *"không cần cái khung viền, cứ đặt card lên khu vực đó thôi"* — viền trong suốt, chỉ hiện vàng khi `hot`. Ảnh chụp 1280×800: nấu Pho Bo → kéo thu → thẻ món nằm thẳng trên mặt bàn góc trái dưới, điểm 2, Pho Bo ✓ trong `#my-order`, tay bốc bù lên 8. Viền `hot` lúc kéo vẫn chưa bắt được. |
| 17 | [BUG] Kéo thẻ không ăn: nguyên nhân là `onPointerDown` bỏ qua mọi cú nhấc thẻ khi client chưa ở bước `play` — ngay sau khi bấm bốc (~320ms, đo được) và sau khi thu món (trong lúc bốc bù). Sửa trong `ui/table.ts`: cờ `expectPlay` (bật khi bấm bốc/thu món, tắt khi nhận `prompt`/`claimOpen`/`end`); khi cờ bật vẫn cho nhấc thẻ; thả vào ô đánh lúc chưa tới bước đánh → `deferPlay` ẩn thẻ, đánh ngay khi prompt tới (tối đa 3s). Host không đổi, không đổi protocol | 2026-09-19 | `design/ui/table.md` (thêm 1 dòng Edge cases, doc vẫn BUILT). Giả thuyết cũ (ngưỡng 6px) **bị loại**: bộ ghi sự kiện tạm cho thấy kéo nhanh bằng chuột thật vẫn có `pointermove` giữa đường, và cả 2 lần kéo bằng chuột thật (vào ô đánh, vào vùng nấu) đều ăn. **Tái hiện trước khi sửa:** bấm bốc, 80ms sau kéo thẻ vào ô đánh → không đánh, tay vẫn 8, không báo gì. **Sau khi sửa (Play vs Bots 2 người, 1280×800 không giả lập):** 3/3 lượt bốc-rồi-kéo-ngay đều đánh được (8 → 7, lượt chuyển); nấu → thu món → 60ms sau kéo vào ô đánh → đánh được sau khi bốc bù xong (tay còn 7); chưa bốc thì vẫn không nhấc được thẻ; `npm run build` sạch, console không lỗi. **Chưa verify / còn mở:** (1) thẻ ma đứng lơ lửng khi kéo bằng chuột thật lúc bật giả lập viewport 1280×800 (ghi ở #16) — không tái hiện khi tắt giả lập, nhiều khả năng do công cụ; (2) viền `hot` của `#my-foods` khi kéo thẻ món qua — vẫn chưa bắt được; (3) online 2 tab; (4) thẻ ma vẽ lệch (−20px, −40px) so với con trỏ nên thả thẻ ở mép dưới vùng nấu có thể trượt — chưa đo, không sửa trong ticket này. **Sửa tiếp (2026-09-19) sau khi người dùng chỉ ra nguyên nhân thứ 2:** *"nếu tôi drag đến chỗ target rồi dừng chuột ở đó thì tôi thả được nhưng nếu tôi đang drag quá khu đó kiểu vẩy chuột ấy thì sẽ detect là thả không được"* — code chỉ xét đúng điểm nhả chuột. Thêm `dropPoint()` trong `ui/table.ts`: giữ đường kéo 150ms cuối (kể cả `getCoalescedEvents`), điểm nhả không có tác dụng thì thử vị trí thẻ đang vẽ rồi lần ngược đường kéo; với thẻ lấy từ tay, nhả trên quạt/thẻ khác trong tay tính là không có tác dụng (lần thử đầu hỏng vì cú vẩy ngang qua vùng nấu kết thúc trên đầu thẻ quạt). Verified (Play vs Bots 2 người, 1280×800): vẩy qua vùng nấu, nhả bên phải → thẻ vào vùng nấu; vẩy qua ô đánh, nhả phía trên → đánh được (8 → 7); kéo chậm ra chỗ trống rồi dừng 400ms → bật về, không gì xảy ra; nhấc thẻ rồi nhả trên quạt → không gì xảy ra; thẻ trên vùng nấu kéo về quạt → về tay; **chuột thật** (không giả lập viewport) vẩy từ quạt qua ô đánh, nhả trên ghế đối thủ → đánh được, trang chỉ nhận 1 `pointermove` giữa đường; `npm run build` sạch, console không lỗi. Chưa verify: vẩy thẻ món vào góc bàn, vẩy thẻ tố. |
| 18 | Rework đầu ván: Bước A `#menuIntroModal` — Menu hiện lần lượt từng món (220ms) + giữ 900ms; Bước B `#orderIntroModal` mới (khung tím `--order-need`, thẻ lật lên) — Order của ghế mình lần lượt từng món (280ms) + câu "Cook every dish on this list to end the match instantly and score a big bonus +10…" + giữ 1400ms; tap 1 lần = hiện hết món còn lại, tap lần 2 = qua bước; đếm giờ dừng khi tab ẩn; bỏ pop-in lần 2 ở `#menu-rail`/`#my-order`; `introMs()` trong `data.ts` dùng chung cho host `wait` và client; gỡ `MENU_REVEAL_MS`/`MENU_ITEM_POP_MS` | 2026-09-19 | `design/10-match-intro.md` → BUILT; dòng cũ ở `ui/menu-orders.md`, `ui/table.md` đánh dấu "THAY bởi 10-". `npm run build` sạch. Verified qua browser (pane bị ẩn nên giả lập `document.hidden=false` trong trang để đo): 4 người không skip — Menu 10 món cách nhau 220ms, Order 5 món cách nhau 280ms, client đóng intro ở ~6,62s khớp host `introMs(10,5)` = 6,60s, rồi rail 10 món + `#my-order` 5 dòng hiện thẳng (0 phần tử `.popin`), chia 7 thẻ; tap ở Bước A → hiện hết ngay, giữ đủ 1,25s, vẫn sang Bước B; tap ở Bước B → hiện hết; tap lần 2 lúc giữ → đóng bước ngay; giả lập tab ẩn → dừng đếm ở đúng món đang hiện; 1024×700 — Menu 10 món 2 hàng vừa khung, không cuộn; online 2 tab (phòng 4 ghế, 2 người + 2 bot) — cùng Menu cùng thứ tự, Bước B mỗi tab chỉ có Order của chính mình, tải lại tab khách giữa ván → không phát lại intro, rail + Order dựng ngay; console không lỗi mới. **Chưa verify:** Play again sau màn kết thúc (cùng đường event `start` đã kiểm nhiều lần, nhưng chưa bấm thật); animation pop/lật nhìn bằng mắt khi chạy — pane ẩn làm CSS animation đứng, chỉ thấy trạng thái cuối và thẻ lật dở; resize giữa intro. Giả định: tap trong lúc giữ kết thúc bước sớm (doc chỉ nói tap = hiện hết món còn lại); host không đổi shape event nên không bump `PREFIX`. |
| 19 | Order intro đóng lại bằng animation thu xuống `#my-order` (góc phải dưới): khung overlay mờ + thu nhỏ, từng ảnh món bay + thu nhỏ về đúng dòng (`ORDER_LAND_MS` 550, so le `ORDER_LAND_STAGGER_MS` 70), dòng hiện kèm 1 nhịp sáng khi ảnh chạm tới; `introMs()` cộng thêm phần thu cho host `wait` | 2026-09-19 | `design/10-match-intro.md` Rule 5–6 → BUILT. `npm run build` sạch. Verified qua browser (1280×800, 4 người, pane ẩn nên dừng animation bằng tay để đo): ảnh bay xuất phát 64px, giữa đường 38px, điểm đáp trùng khít từng dòng `#my-order` (left/top/height khớp tới px) cho cả 5 món; sau khi thu: 5 dòng Order hiện, rail 10 món, chia 7 thẻ, chồng bài sáng chờ bốc; console không lỗi. Sửa trong lúc test: đo vị trí xuất phát khi thẻ đang lật dở (skip giữa chừng) làm ảnh bay sai cỡ → tắt animation lật trước khi đo. **Chưa verify:** nhìn tận mắt chuyển động mượt (pane ẩn, CSS animation không chạy); online 2 tab (không đổi dữ liệu mạng, chỉ đổi thời gian chờ host); 1024×700. |
| 20 | Menu intro đóng lại bằng animation thu lên `#menu-rail` (trên cùng): khung mờ + thu nhỏ, từng ảnh món bay + thu nhỏ về đúng ô (`MENU_LAND_MS` 500, so le `MENU_LAND_STAGGER_MS` 40), ô hiện kèm 1 vòng sáng vàng khi ảnh chạm tới; gộp với #19 thành 1 hàm `introLand` dùng chung; tách cờ ẩn rail/Order để Order vẫn ẩn trong lúc Menu bay lên; `introMs()` cộng thêm phần thu Menu | 2026-09-19 | `design/10-match-intro.md` Rule 3, 6 → BUILT. `npm run build` sạch. Verified qua browser (1280×800, 4 người, pane ẩn nên tua animation tới cuối bằng tay để đo): cả 10 ảnh Menu đáp trùng khít ô ảnh trong `#menu-rail` (lệch 0px vị trí + chiều cao), 5 ảnh Order vẫn đáp khít dòng `#my-order` (không vỡ #19); `#my-order` rỗng trong lúc Menu bay; nhịp khớp `introMs` (Menu bắt đầu bay ~3,27s, Order ~7,28s); xong intro không còn ảnh bay hay overlay sót, chia 7 thẻ; console không lỗi. **Chưa verify:** nhìn tận mắt độ mượt (pane ẩn), online 2 tab, 1024×700. |
| 21 | Nút Cook chỉ ghi `COOK`; đóng quạt gom từng thẻ về thẻ giữa, nhóm không di chuyển; thẻ món hiện ngay tại tâm (bỏ trượt `--x`); `T.fanClose` 260→450ms | 2026-09-19 | `design/ui/table.md` Tuning pass (8). Verified qua browser (2 người): nút hiện `COOK`; đo 4 thẻ Mì Quảng từ tâm 452/492/532/572 → cùng 512 = tâm nhóm, `left` nhóm giữ 414 suốt pha gập. Snap `--x` sau khi ra thẻ món: kiểm chứng cơ chế trên `.hgroup` thật (transition bị `finish()` ngay), chưa quan sát lại end-to-end sau fix |
| 22 | Nút `COOK` hiện ngay giữa: bỏ keyframe `popin` trùng tên (bản sau dùng `transform` ghi đè `translateX(-50%)`), nút giữ nguyên qua render (pop-in chạy 1 lần), căn giữa bằng `translate` thay `transform` (scale pop-in từng co offset → lệch 13px). Gập quạt tự chạy khi hết `COOK_MS` cả lúc tố thắng (trước chờ `reveal` ~1.25s), mỗi stack gập 1 lần. Điểm thả + hint theo tâm thẻ đang vẽ trước, con trỏ sau | 2026-09-19 | `design/ui/table.md` Tuning pass (9). Verified qua browser (2 người): thả thẻ với con trỏ nằm trong vùng nấu (y 500 > 475) nhưng thẻ trên ô Play → đánh ra đúng, ô Play sáng khi kéo. Nút: đo trên nút thật trong `#prep` — lệch 0px suốt pop-in (cách cũ 13px). **Chưa quan sát end-to-end:** gập quạt lúc tố thắng (không có dịp tố trong ván test sau fix) và nút giữ qua render khi ghép món thật |
